apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ska.fullname" . }}-test-connection"
  labels:
    {{- include "ska.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ska.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
